Fix and finalize the knob navigation component, using the current 
visual state as the exact baseline to preserve:

BASELINE APPEARANCE (do not deviate from this look):
- Dark semicircle plate hugging the left screen edge, curved right 
  boundary, subtle inset shadow giving it a recessed metal-panel feel.
- Glossy dark dome knob with a faceted metallic blade pointer, 
  positioned at the plate's pivot (screen edge), rotating to aim at 
  each tick.
- Thin tick marks running along the arc; only major ticks are bold, 
  minor ticks are faint hairlines.
- Labels sit beside their ticks, upright text (counter-rotated to the 
  arc angle): the active label (e.g. "IPSUM") is bold and fully 
  opaque white; adjacent labels (e.g. "DOLOR", "SIT AMET") are dimmer 
  gray; labels near the top/bottom edges of the plate are partially 
  clipped/cut off (e.g. "LOREM", "CONSEC...") to reinforce that more 
  content exists off-frame.
- Index counter (e.g. "01/05") in small serif text near the top-left 
  of the plate.
- Panel content to the right shows the current section's title in 
  large bold type and body copy below it.

FIXES TO APPLY ON TOP OF THIS BASELINE:

1. VISIBLE ON FIRST LOAD, HIDDEN THEREAFTER
On initial page load, the knob assembly (plate + dome + blade + 
labels) must render fully visible and open exactly as shown in the 
baseline — no hover needed to see it. After a short delay (or after 
the user's first interaction, whichever happens first), it should 
retract off-screen using the existing hover-trigger slide behavior. 
Once retracted, it only reappears when the cursor approaches the left 
-edge trigger zone.

2. FIX ACCESSIBILITY / HIT-AREA ISSUES
Most of the knob and plate currently do not respond reliably to 
clicks/hover. Audit and fix so that:
   - The full visible dome, blade, and hub area is clickable, not 
     just a small central point.
   - Every visible label/tick is independently clickable without the 
     knob or plate blocking pointer events.
   - The hover-trigger zone is wide/tall enough to reliably catch 
     mouse movement near the left edge, no dead zones.
   - Add keyboard accessibility: knob and visible labels get 
     tabindex, operable via Enter/Space and Arrow keys, with a 
     visible focus outline.

3. EXACT KNOB FEATURE (physical dial behavior)
Preserve the current rotation mechanic exactly — dome, blade, and hub 
rotate together as one rigid unit, landing precisely on each tick's 
angle, matching the baseline screenshot's pointer style — but add:
   - A "click-in" press effect: on mousedown, the knob scales down 
     slightly (~0.96) with its shadow compressing inward, springing 
     back to full scale on release.
   - A directional glow trail along the arc just swept, fading out 
     over ~400ms.
   - Continuous forward-only rotation for wraparound (last → first 
     continues forward rather than snapping backward) — already 
     implemented, must not regress.

Verify the final result visually matches the baseline screenshot's 
proportions, opacity levels, and clipping behavior for labels, while 
being fully clickable/keyboard-navigable and visible-then-hiding as 
specified.