You are building a premium personal portfolio for "Kailash Murali T", student developer at VIT Chennai.

=== TECH STACK ===
Next.js 14 (App Router) + TypeScript + Tailwind CSS + Framer Motion
- `create-next-app` base
- Framer Motion for all animations
- Tailwind CSS for all styling
- Google Fonts via next/font: Bebas Neue (display), Inter (body)
- No UI component libraries — build everything from scratch

=== DESIGN PHILOSOPHY ===
STRICT BLACK AND WHITE ONLY.
- Dark mode default: bg #0a0a0a, text #f0f0ec
- Light mode: bg #f0f0ec, text #0a0a0a
- No colors, gradients, or hues — only varying opacities of black/white
- CSS variables: --bg, --fg, --muted, --border, --card
- transition: all 0.4s ease on all elements

=== LOADING SCREEN ===
Full viewport overlay on first load:
- Background: var(--bg)
- Center: "KM" in Bebas Neue 120px, color var(--fg)
- Below: 1px horizontal line animating width 0% → 100% over 1.8s ease-in-out
- Below line: counter "000" → "100" incrementing over 1.8s
- After 2.2s: overlay slides UP (translateY 0 → -100%), 0.6s ease-in-out
- Framer Motion: AnimatePresence + motion.div for mount/unmount
- Body scroll locked during load

=== HERO ENTRANCE ANIMATION (CRITICAL) ===
After the loader exits (at ~2.2s), the hero content must animate IN automatically
WITHOUT requiring the user to scroll. This is a pure time-based entrance, not
scroll-triggered. Use Framer Motion with absolute delays keyed off the loader exit:

Sequence (all easing: easeOut, duration: 0.9s each):
  1. The background marquee fades in: opacity 0 → 0.06, delay: 2.3s
  2. Name "KAILASH MURALI T": y: 60px → 0, opacity: 0 → 1, delay: 2.5s
  3. Role tags row: y: 20px → 0, opacity: 0 → 1, delay: 2.8s, stagger 0.08s per tag
  4. Scroll cue line: opacity 0 → 1, delay: 3.4s

DO NOT use IntersectionObserver or useInView for any hero element.
Use Framer Motion `animate` prop with `transition: { delay: X }` directly.
The page should feel like a cinematic reveal — loader exits → name sweeps up → tags appear.

=== NAVBAR ===
Fixed. Appears (fade + slide down) only after scrolling past 60px.

DARK MODE:
- bg rgba(10,10,10,0.82), backdrop-filter blur(14px)
- border-bottom: 1px solid var(--border)
- All text: var(--fg) = #f0f0ec

LIGHT MODE:
- bg rgba(240,240,236,0.92), backdrop-filter blur(14px)
- border-bottom: 1px solid rgba(10,10,10,0.10)
- All text: color #0a0a0a, opacity 1 — explicitly set, NOT inherited
- Nav links: color: #0a0a0a on both default and hover states
- Toggle button ☯: border 1px solid rgba(10,10,10,0.25), color: #0a0a0a

STRUCTURE:
- Left: "KM" Bebas Neue 20px — K in var(--fg), M in var(--muted)
- Center: About · Projects · Experience · Contact — 11px uppercase tracked
- Right: ☯ (U+262F, 18px) in a 36px circle border button — ONLY mode toggle on the page
- Mobile: hamburger replaces center links

=== HERO SECTION ===
Full viewport height. Background: var(--bg). overflow: hidden.

LAYER 1 — Background marquee watermark (position absolute, inset 0, z-index 0):

SMOOTHNESS REQUIREMENTS (implement ALL of these):
  · Each .mq-track contains the word set FOUR times (not two) to guarantee
    no visible seam even on ultrawide screens
  · translateX(0) → translateX(-25%) for a 4x-duplicated track
    (this is equivalent to -50% on a 2x track but mathematically cleaner for 4x)
  · animation-timing-function: linear — absolutely no easing, no acceleration
  · will-change: transform on every .mq-track for GPU compositing
  · The animation duration is 40s (slower = smoother feel) for rows 1,3,5
    and 34s for rows 2,4 (slight speed difference adds depth)
  · No gap between repetitions: padding on each span is exactly 0 3.5rem, nothing else
  · Rows 1,3,5 direction: normal (left). Rows 2,4 direction: reverse (right).
  · pointer-events: none, user-select: none on the entire marquee container

COMPACTNESS REQUIREMENTS:
  · Only 4 rows total (not 5) — evenly spaced via justify-content: space-around
  · Font size: clamp(2.4rem, 4.5vw, 4rem) — smaller than before, more subtle
  · opacity: 0.055 dark mode, 0.04 light mode — even more ghostly
  · Text: alternate italic "Welcome" and upright "வணக்கம்" per span
    italic "Welcome" must use font-style: italic
    "வணக்கம்" uses font-style: normal

LAYER 2 — Foreground (position relative, z-index 1):
All content left-aligned (not centered). padding-left: clamp(40px, 6vw, 96px).
  · "KAILASH MURALI T" — Bebas Neue, clamp(80px, 12vw, 160px), line-height 0.88,
    letter-spacing 0.02em, color var(--fg)
  · Role tags row — three items separated by a thin 1px vertical rule:
    "Developer"  |  "Quizzer"  |  "Sports Enthusiast"
    Inter 12px, uppercase, letter-spacing 0.18em, color var(--muted)
    margin-top: 20px
    No dots, no bullets — just the 1px rgba(fg, 0.25) vertical dividers between them
  · Scroll cue: absolute bottom-center, 1px line fading to transparent, breathing anim

=== ABOUT SECTION ===
Two columns: left = image, right = heading + text.

IMAGE TREATMENT (match the reference photo — portrait, slightly angled):
  · Container: position relative, width fit-content
  · The actual <img>: width 300px, height 400px, object-fit cover, object-position top
    filter: grayscale(100%), transition filter 0.5s
    On hover: grayscale(0%)
  · Image frame shape: use clip-path: polygon(0 0, 100% 0, 96% 92%, 88% 100%, 0 100%)
    — a 5-point polygon: square top-left, square top-right, slight inward cut
    bottom-right corner and bottom-left corner, giving a subtle angular cut
    at the bottom-right only (like a dog-ear fold). This matches the portrait reference.
  · Ghost border: position absolute, inset 0, border 1px solid var(--border),
    same clip-path as the image, pointer-events none,
    offset: transform translate(14px, 14px) — shifted right and down (not left/below)
  · No second ghost box. One clean offset border only.
  · The image and ghost border sit inside a relative wrapper that has
    padding-bottom: 14px, padding-right: 14px to accommodate the offset

RIGHT COLUMN:
  · "WHO I AM" Bebas Neue clamp(48px, 7vw, 96px)
  · Paragraph text (Inter, 1rem, line-height 1.9, color var(--muted)):
    "I'm Kailash Murali T — a Computer Science (AI & ML) student at VIT Chennai.
    I work at the intersection of machine learning, systems design, and product thinking.
    I've interned at Bray International and Ashok Leyland, led finance for large-scale fests,
    and serve as Treasurer of the VITC Quiz Club. Outside code, I'm a passionate
    sports enthusiast and quizzer."
  · Strong tags around "Bray International", "Ashok Leyland", "VITC Quiz Club"

Framer Motion scroll reveal per column, stagger 0.1s

=== SKILLS SECTION ===
REMOVE ENTIRELY. No skills section, no pills, no nav link for it.

=== PROJECTS SECTION ===
Heading: "WHAT I'VE BUILT"
Grid: repeat(auto-fill, minmax(320px, 1fr)), gap 24px

Cards:

Card 01 — "Bray-PPE"
  Tags: Python · YOLOv8 · Azure · React
  Description: "Real-time YOLOv8 computer vision pipeline for automated PPE compliance
  detection at Bray International. Azure-hosted backend with React dashboard delivering
  low-latency safety violation alerts."
  NO "View Repo" link — private project. Instead show a small pill badge: "Private · Bray International"

Card 02 — "Bray-LTP"
  Tags: Python · LightGBM · Azure · React
  Description: "Supply chain lead time predictor using LightGBM on historical buyer-seller
  transaction dyads. Deployed on Azure with a custom React dashboard for operational decisions."
  NO "View Repo" link — private project. Instead show pill badge: "Private · Bray International"

Card 03 — "Kaavalan"
  Tags: Python · Computer Vision · Surveillance
  Description: "A smart surveillance system combining real-time object detection and
  intelligent alerting to enhance security monitoring."
  Link: https://github.com/Kailash-Murali/kaavalan (label: "View Repo →")

Card 04 — "Secure-Stego-UPI"
  Tags: PyTorch · DCGAN · Cryptography · Biometrics
  Description: "Coverless GAN steganography fused with multi-factor biometric authentication
  for tamper-evident UPI transaction verification."
  Link: https://github.com/V4run05/Secure-Stego-UPI (label: "View Repo →")
  Collab badge: small bordered pill "w/ V4run05" in var(--muted) at bottom of card

Card 05 — "Aram AI"
  Tags: Python · Agentic AI · LegalTech
  Description: "Explainable agentic AI for the Indian legal domain — a continuation of
  the learn-law initiative, built to make law accessible through conversational AI."
  Link: https://github.com/Kailash-Murali/learn-law-2.0 (label: "View Repo →")

All cards:
- border 1px solid var(--border), border-radius 12px, bg var(--card)
- Large number top-right: Bebas Neue 3rem, color var(--border)
- Hover: translateY(-4px), border-color rgba(fg, 0.4)
- Framer Motion stagger 0.1s on scroll enter

=== EXPERIENCE SECTION ===
Heading: "WHERE I'VE BEEN"
Nav link: "Experience"

Two-column layout on desktop, stacked on mobile:

LEFT COLUMN — Work Experience (vertical timeline, 1px line + dot markers):

  Entry 1:
    Date: May 2026 – Jul 2026
    Role: AI Solutions Engineer
    Org: Bray International Inc., Sriperumbudur
    Detail: "Engineered YOLOv8 pipeline for PPE compliance and a LightGBM lead time
    predictor — both deployed on Azure with React dashboards."

  Entry 2:
    Date: May 2025 – Jun 2025
    Role: Analytics Intern
    Org: Ashok Leyland, Chennai
    Detail: "Built automated vehicle classification with fuzzy matching algorithms.
    Contributed to AI governance policies aligned with ISO 42001."

RIGHT COLUMN — Leadership (same visual style):

  Entry 1:
    Date: Feb 2026
    Role: Finance Organiser
    Org: Vibrance 2026, VIT Chennai
    Detail: "Headed team of 8 managing finances for 140+ events."

  Entry 2:
    Date: Oct 2025
    Role: Finance Coordinator
    Org: TechnoVIT 2025, VIT Chennai
    Detail: "Coordinated financial planning for 120+ club events."

  Entry 3:
    Date: Jul 2025 – Mar 2026
    Role: Treasurer
    Org: VITC Quiz Club, Chennai

DO NOT include education, CGPA, or schooling details anywhere.

RESUME DOWNLOAD BUTTON (centered, below both columns):
- Text: "Download Résumé ↓"
- Style: Bebas Neue 15px, letter-spacing 0.15em, uppercase
- border 1px solid var(--border), padding 16px 48px, border-radius 2px, bg none
- Hover: bg var(--fg), color var(--bg), transition 0.3s
- href="./Resume.pdf" download="Kailash_Murali_Resume.pdf"
- Framer Motion: whileHover scale 1.02

=== CONTACT SECTION ===
Two columns: info links + form
Info:
- Email: kailashmurali17@gmail.com
- LinkedIn: https://www.linkedin.com/in/kailash-murali-t-66721b28b/
- GitHub: https://github.com/Kailash-Murali
All with inline SVG icons (no icon libraries).
Form: bottom-border only inputs, inline validation, mailto fallback.

=== FOOTER ===
"© 2026 Kailash Murali T  ·  Made with intention"
11px, var(--muted), centered.

=== CUSTOM CURSOR ===
- 10px filled dot (bg var(--fg)) + 36px ring (border 1px solid rgba(fg, 0.45))
- Ring expands to 56px on hover over interactive elements
- cursor: none on body

=== SCROLL REVEAL ===
Framer Motion useInView for ALL sections EXCEPT hero.
Hero uses time-based animation only (see HERO ENTRANCE ANIMATION above).
Default hidden: opacity 0, y 40. Visible: opacity 1, y 0. Duration 0.7s.
Odd timeline entries slide from LEFT (x: -40 → 0), even from RIGHT (x: 40 → 0).

=== README ===
Update README.md:
- Stack: Next.js 14, TypeScript, Tailwind CSS, Framer Motion
- Sections: Loading Screen → Hero (auto-entrance) → About → Projects → Experience → Contact
- Projects listed: Bray-PPE (private), Bray-LTP (private), Kaavalan, Secure-Stego-UPI (collab V4run05), Aram AI
- Removed: Skills section, education block, Old Portfolio card
- Features: cinematic loader → auto hero entrance, resume download, ☯ dark/light toggle, custom cursor
- Run: `npm install` → `npm run dev` → localhost:3000

=== HARD RULES ===
- Zero color — only black, white, their opacities
- ☯ toggle appears ONLY in the navbar — nowhere else
- No external icon or component libraries
- No lorem ipsum — all copy is real
- Hero content animates on a time delay, NEVER on scroll
- Output: complete working Next.js project with all files